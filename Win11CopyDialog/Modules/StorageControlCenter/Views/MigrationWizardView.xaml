<UserControl x:Class="Win11CopyDialog.Modules.StorageControlCenter.Views.MigrationWizardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="StepTextBlockStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
            <Setter Property="Margin" Value="0,4,0,4"/>
        </Style>
        <Style x:Key="ActiveStepTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource StepTextBlockStyle}">
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style x:Key="CompletedStepTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource StepTextBlockStyle}">
            <Setter Property="Foreground" Value="#10B981"/> <!-- Green for completed -->
            <Setter Property="TextDecorations" Value="Strikethrough"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel: 10-Step Wizard Tracker -->
        <Border Grid.Column="0" Style="{StaticResource Card}" Padding="16" Margin="0,0,10,0">
            <StackPanel x:Name="StepsPanel">
                <TextBlock Text="🛠 ПРОЦЕСС МИГРАЦИИ" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,12"/>
                
                <TextBlock x:Name="Step1Text" Text="1. Инициализация" Style="{StaticResource StepTextBlockStyle}"/>
                <TextBlock x:Name="Step2Text" Text="2. Анализ и Preflight" Style="{StaticResource StepTextBlockStyle}"/>
                <TextBlock x:Name="Step3Text" Text="3. Подготовка целевого диска" Style="{StaticResource StepTextBlockStyle}"/>
                <TextBlock x:Name="Step4Text" Text="4. Создание снимка VSS" Style="{StaticResource StepTextBlockStyle}"/>
                <TextBlock x:Name="Step5Text" Text="5. Копирование данных" Style="{StaticResource StepTextBlockStyle}"/>
                <TextBlock x:Name="Step6Text" Text="6. Настройка загрузчика" Style="{StaticResource StepTextBlockStyle}"/>
                <TextBlock x:Name="Step7Text" Text="7. Верификация целостности" Style="{StaticResource StepTextBlockStyle}"/>
                <TextBlock x:Name="Step8Text" Text="8. Финализация" Style="{StaticResource StepTextBlockStyle}"/>
                
                <Separator Background="{DynamicResource GlassBorderBrush}" Margin="0,16,0,16"/>
                
                <TextBlock x:Name="OverallProgressPercentText" Text="0%" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center"/>
                <ProgressBar x:Name="OverallProgressBar" Height="8" Minimum="0" Maximum="100" Value="0" Margin="0,8,0,0" Foreground="{DynamicResource AccentBrush}"/>
            </StackPanel>
        </Border>

        <!-- Right Panel: Controls and Logs -->
        <Border Grid.Column="1" Style="{StaticResource Card}" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,16">
                    <TextBlock Text="Клонирование работающей ОС (Live Migration Wizard)" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,8"/>
                    <TextBlock Text="Система выполнит безопасный перенос текущей работающей Windows на выбранный накопитель с использованием службы теневого копирования (VSS)." FontSize="12" Style="{StaticResource MutedText}" TextWrapping="Wrap"/>
                </StackPanel>

                <!-- Disk Selection and Warning -->
                <StackPanel Grid.Row="1" x:Name="ConfigurationPanel" Margin="0,0,0,16">
                    <TextBlock Text="Выберите целевой накопитель:" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,8"/>
                    <ComboBox x:Name="TargetDriveCombo" Height="32" FontSize="13" Margin="0,0,0,12"/>

                    <Border Background="#2A0F11" BorderBrush="#EF4444" BorderThickness="1" CornerRadius="6" Padding="12">
                        <StackPanel>
                            <TextBlock Text="⚠️ КРИТИЧЕСКОЕ ВНИМАНИЕ" FontSize="12" FontWeight="Bold" Foreground="#EF4444" Margin="0,0,0,4"/>
                            <TextBlock Text="Все данные, разделы и файлы на выбранном целевом диске будут БЕЗВОЗВРАТНО уничтожены. Операция не может быть отменена после начала этапа подготовки диска. Пожалуйста, дважды проверьте выбранный диск перед началом." FontSize="11" Foreground="#FCA5A5" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Console Output -->
                <GroupBox Grid.Row="2" Header="Журнал миграции" Foreground="{DynamicResource MutedTextBrush}" BorderBrush="{DynamicResource GlassBorderBrush}">
                    <TextBox x:Name="LogTextBox" Background="{DynamicResource WindowBackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}" 
                             FontFamily="Consolas" FontSize="11" IsReadOnly="True" VerticalScrollBarVisibility="Auto" 
                             BorderThickness="0" Margin="2" TextWrapping="Wrap"/>
                </GroupBox>

                <!-- Action Buttons -->
                <DockPanel Grid.Row="3" Margin="0,16,0,0">
                    <Button x:Name="TestModeCheckBox" Content="Включить тестовый режим (Dry Run)" VerticalAlignment="Center" Foreground="{DynamicResource MutedTextBrush}" Background="Transparent" BorderThickness="0" Click="TestMode_Click"/>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="CancelBtn" Content="Отмена" Width="100" Height="32" Margin="0,0,8,0" Click="CancelBtn_Click"/>
                        <Button x:Name="StartBtn" Style="{StaticResource CyberButton}" Content="🚀 НАЧАТЬ ПЕРЕНОС ОС" Width="180" Height="36" FontWeight="Bold" Click="StartBtn_Click"/>
                    </StackPanel>
                </DockPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
